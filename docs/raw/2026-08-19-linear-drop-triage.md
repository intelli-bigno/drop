# Linear DROP 이슈 전수 정리 (2026-08-19)

지시: "linear에 남아 있는 drop 관련 이슈 하나도 남겨놓지말고 모두 처리해. 정말 애매한건 뷰를 만들어서 구체화 요구해."
범위: BRU 팀의 미완(Backlog/Todo/In Progress) 이슈 전건. 종결(Done/Canceled/Duplicate)은 손대지 않았다.
근거 컨벤션: INT "이슈 컨벤션 — 라이프사이클 게이트" (slugId `fc050897a60d`)

## 처리 결과 — 21건

### Ready 판정 → Todo + `루프 › triaged` (14건)
| 이슈 | 우선순위 | 트리아지에서 정한 것 |
|---|---|---|
| BRU-59 데스크톱 포커스 viewer | High | BRU-66(원문 변형)과 직결 → High 상향 |
| BRU-77 모바일 탭=뷰어 | High | 같은 이유로 High |
| BRU-85 j/k 스크롤 회귀 | High | 재현 절차·증거 완비 |
| BRU-81 태그 속도 | High | 캡처 경로 전체에 곱해짐 → High. 실측 선행 강제 |
| BRU-57 호버 오버레이가 태그 덮음 | Medium | BRU-82와 한 PR로 |
| BRU-82 액션 아이콘 hover 전용 | Medium | BRU-57보다 먼저 |
| BRU-80 다중 선택·일괄 수정 | Medium | 액션 세트는 BRU-15와 동일 |
| BRU-67 hasLink 플래그 | Medium | 백필 포함 — Supabase 워크플로우 준수 |
| BRU-84 전역 단축키 | Medium | 기본 조합 **⌥Space** 제안 |
| BRU-76 Android 팔레트 | Medium | 미결 결정 없음 |
| BRU-37 마크다운 (기존 Todo) | Medium | 렌더러 = DropCore 자체 파서로 확정 |
| BRU-79 전체 펼쳐보기 토글 | Low | 토글 상태 **세션 간 미유지**로 확정 |
| BRU-43 iOS 위젯 딥링크 | Low | 선행 BRU-48 Done — 차단 해소 |
| BRU-86 댓글 4/4 Android **(신설)** | Low | 아래 참조 |

### 트랙 3건 — 상태 정정 + 잔여 확정
- **BRU-72 디자인 통일**: Backlog인데 자식 1/4·2/4가 Done → **In Progress로 정정**. 트랙을 막는 건 BRU-75의 배포 타깃 결정 하나.
- **BRU-61 댓글**: 자식 62·63·64 전부 Done인데 본문 범위의 Android만 이슈가 없었다. 본문은 "BRU-40/41에 함께 넣는다"였으나 **둘 다 Done인 채 댓글은 안 들어갔다** → **BRU-86 신설**로 구멍을 메움.
- **BRU-36 Android 트랙**: 자식 38~42·68 전부 Done. 잔여는 BRU-76·86·77·30 넷으로 확정. 실기 패리티 검증은 **하지 않았다**.

### 결정 대기 → `루프 › needs-decision` + Backlog (5건)
컨벤션 §11대로 Backlog로 되돌리고, 각 이슈에 **선택지를 담은 질문 코멘트**를 남겼다.

| 이슈 | 물어본 것 |
|---|---|
| BRU-75 iOS 디자인 | 배포 타깃 iOS 17 유지(a) vs iOS 26 상향(b) — 본문은 (b) 유력 |
| BRU-83 Project 개념 | 전용 엔티티(a) vs 예약 태그 네임스페이스(b) + 복수 소속 허용 여부 |
| BRU-28 앱 이름 DROP | 안 바꿈 / 표시 이름만 / 식별자까지 — 추천은 표시 이름만 |
| BRU-30 Android OAuth | GCP 콘솔 수동 작업(사람만 가능) 지금 가능한가 — 8일간 In Progress였으나 진행 증거 없어 Backlog로 되돌림 |
| BRU-52 마인드맵 조사 | DROP 앱 이슈 아님·프로젝트 미지정 미아 — 지금/나중/취소 |

**커스텀 뷰**: "DROP — 결정 대기 (구체화 필요)" — https://linear.app/intellieffect/view/11c46a9f3cd6
(GraphQL `customViewCreate`, 필터 `label = needs-decision`, 프로젝트 그룹핑, shared)

### 손대지 않은 것
- BRU-1 (Anthropic 자격증) — DROP 무관, 개인 할 일

## 하지 않은 것 (정직 기록)
- 코드 구현은 한 건도 하지 않았다. 이번 작업은 **상태·판정·질문**까지다.
- 실기/시뮬레이터 실측 검증 없음. 자식 Done 여부는 Linear 상태로만 확인했다.
- BRU 팀에는 `type:*`·`area:*` 라벨이 존재하지 않아 컨벤션 §2의 라벨 요건은 충족하지 못했다 (워크스페이스 라벨은 Intellieffect 팀 소속).

---

## 2차: 전건 병렬 구현 착수 (2026-08-19)

지시: "그냥 다 처리해 병렬로 돌려" → 결정 대기 5건을 추천안대로 확정하고, 11개 에이전트를 워크트리 격리로 동시 실행.

### 확정한 결정 (사용자 위임)
| 이슈 | 확정 |
|---|---|
| BRU-75 | **(b) 배포 타깃 iOS 26 상향** — 코드 한 갈래 |
| BRU-83 | **(a) 전용 `projects` 테이블 + `notes.project_id`**, 단일 소속 |
| BRU-28 | **표시 이름만 DROP** — appId·`drop://`·`@brxce/drop-mcp`·토큰 접두사 전부 불변 |
| BRU-52 | 조사 수행 후 도입 여부만 사람에게 남김 |
| BRU-30 | **확정 불가 — 사람만 가능** (GCP 콘솔, API 없음) |

### 에이전트 배치 (충돌 회피를 위해 파일 영역별로 묶음)
1. 데스크톱 카드 — BRU-82·57·59·79
2. 데스크톱 키보드 — BRU-85·80
3. 데스크톱 성능 — BRU-81 (실측 선행)
4. Electron — BRU-84·28
5. 백엔드 — BRU-67 (hasLink + 백필)
6. DB 기능 — BRU-83 (마이그레이션 + RLS + 데스크톱 UI)
7. iOS 디자인 — BRU-75
8. iOS 기능 — BRU-77(iOS)·43
9. iOS 마크다운 — BRU-37 (파서는 DropCore)
10. Android — BRU-76·86·77(Android)
11. 조사 — BRU-52

각 에이전트에 부여한 공통 규칙: TDD, 구조/동작 커밋 분리, 테스트 통과 후에만 커밋, Supabase 워크플로우 준수, Linear 착수·완료 코멘트와 증거, PR까지만(머지 금지).

## 병렬 구현 결과 — 11/11 완료, PR 10건 (2026-08-19)

| PR | 이슈 | 핵심 |
|---|---|---|
| #70 | BRU-67 | hasLink 계산을 DB 트리거 한 곳으로. 원인 = `/gi` 정규식 `lastIndex` 잔존(하나 걸러 하나 false) + MCP·모바일은 계산 자체를 안 함 |
| #71 | BRU-85 BRU-80 | scrollTop을 스크롤 컨테이너가 아닌 부모 div에 대입하고 있었다. 다중 선택 신규 |
| #72 | BRU-84 BRU-28 | ⌥Space 전역 단축키(설정 가능·실패 알림), 표시 이름만 DROP |
| #73 | BRU-82 BRU-57 BRU-59 BRU-79 | 오버레이 자리 예약(겹침 +142px → −8px), 카드 3상태, 뷰어는 Lexical 미마운트 |
| #74 | BRU-75 | iOS 26 상향, 시스템색 잔존 47 → 0, 디자인 감사 테스트 신설 |
| #75 | BRU-81 | 병목 = 요청 왕복(직렬 3회 346ms). 낙관적 업데이트로 부착 322ms → 6ms |
| #76 | BRU-83 | projects 테이블 + RLS + 소유자 검증 트리거. Playwright로 화면 왕복 실측 |
| #77 | BRU-76 BRU-86 BRU-77 | Android 팔레트·댓글·뷰어. 다이내믹 컬러는 호출부 자체를 두지 않음 |
| #78 | BRU-77 BRU-43 | iOS 뷰어 + 카메라·갤러리 딥링크. DB 불변 + 민감도 대조군 실측 |
| #79 | BRU-37 | DropCore 마크다운 파서(67 테스트). `#태그`·`snake_case` 오검출 방지 |

전건 MERGEABLE. 머지 순서: #70 → #73 → #71 → #75 → #72 → #76 → #74 → #78 → #79 → #77.

### 남은 위험
- **#74 머지 전 CI Xcode 버전 확인 필수** — 러너가 Xcode 26 미만이면 이 PR부터 iOS 빌드가 깨진다(에이전트가 러너 이미지를 실측 못 함).
- #70의 백필 표는 프로덕션 본문을 로컬에 복제한 측정이다. 진짜 완료 증거는 머지 후 `supabase db push` + `list_notes` 재확인.
- BRU-76의 "위젯 스크린샷" 기준 **미충족** — Glance 렌더를 사람이 눈으로 봐야 한다.
- 대부분의 PR이 앱을 띄운 육안 확인 없이 자동화 실측에만 기댔다. #76·#78만 실제 화면을 조작했다.

### 병렬 실행에서 실제로 터진 사고 3건 (다음에 대비)
1. 로컬 Supabase 컨테이너 공유 → 서로의 `db reset`이 마이그레이션을 날림
2. 공용 스크래치패드 파일명 충돌 → 남의 스크립트를 실행해 **BRU-83에 빈 코멘트**가 달림(감지·삭제됨)
3. 시뮬레이터 공유 → 다른 세션이 조작 중인 화면이 스크린샷에 찍힘

교훈: 병렬 에이전트에게는 **워크트리만이 아니라 스크래치패드·시뮬레이터·로컬 DB도 격리**해 줘야 한다.

### 남은 사람 몫
- BRU-30 Android OAuth — GCP 콘솔은 API가 없어 사람만 가능. `google-services.json`만 받으면 나머지 자동.
- BRU-52 마인드맵 — Canceled (Bruce 지시).
