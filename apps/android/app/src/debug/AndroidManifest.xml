<?xml version="1.0" encoding="utf-8"?>
<!--
  디버그 빌드에만 얹히는 조각 (릴리스 APK에는 들어가지 않는다).

  로컬 Supabase는 http 로 뜨고 에뮬레이터에서는 http://10.0.2.2:58321 로 보인다.
  targetSdk 28 부터 평문 HTTP가 기본 차단이라, 이게 없으면 `make android-config`
  (local) 로 만든 빌드는 에뮬레이터에서 **모든 요청이 실패한다** — 로컬에서
  화면을 실제로 확인할 길이 막힌다. 리모트(https)만 쓸 때는 영향이 없다.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application android:usesCleartextTraffic="true" tools:ignore="UnusedAttribute" />

</manifest>
