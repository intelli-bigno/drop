<?xml version="1.0" encoding="utf-8"?>
<!--
  창 배경만을 위한 색 (BRU-76).

  Compose가 그리기 시작하기 **전에** 시스템이 칠하는 한 겹이라 XML 리소스여야 하고,
  그래서 DropTokens.kt를 참조할 수 없다. 값은 토큰의 `bg-primary` 와 같아야 한다
  (정본: design-system/drop/tokens.json). 여기 두 줄 말고 앱 안의 어떤 색도
  리터럴로 적지 않는다 — 화면은 전부 MaterialTheme.colorScheme 을 쓴다.
-->
<resources>
    <color name="drop_window_background">#F7F6F3</color>
</resources>
