<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      다크에서는 부모도 바꾼다. Light 부모를 그대로 두면 시스템이 상태바 아이콘을
      어두운 색으로 그려 어두운 배경 위에서 보이지 않는다.
      (AppCompat의 DayNight 를 쓰지 않는 앱이라 values-night 로 나눈다.)
    -->
    <style name="Theme.Drop" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/drop_window_background</item>
    </style>
</resources>
