<?xml version="1.0" encoding="utf-8"?>
<!--
  위젯 갤러리에 보이는 미리보기. 이것이 없으면 갤러리가 **앱 아이콘**을 대신 띄운다 —
  사용자는 위젯이 무엇을 보여 주는지 모른 채 놓아야 한다 (실측으로 확인했다).
  실제 위젯은 Glance가 그리고, 이 레이아웃은 정적인 모양만 흉내낸다.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="?android:attr/colorBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="DROP"
            android:textSize="14sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="＋"
            android:textSize="18sp" />
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/widget_preview_first_note"
        android:textSize="13sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:ellipsize="end"
        android:text="@string/widget_preview_second_note"
        android:textSize="13sp" />
</LinearLayout>
