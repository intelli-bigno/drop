<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<!-- 홈 화면·설정에 보이는 이름 (BRU-178). `flutter create --project-name mobile`이
	     남긴 "Mobile"/"mobile"을 네이티브 앱(apps/ios/Drop/Info.plist)과 같은 DROP으로
	     맞춘다 — 같은 번들 ID를 쓰므로 두 앱의 표시 이름이 갈리면 안 된다. -->
	<key>CFBundleDisplayName</key>
	<string>DROP</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>DROP</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<!-- 수출 규정 준수 선언 (BRU-173). 이 키가 없으면 업로드된 빌드가
	     Missing Compliance에 걸려 테스터에게 배포되지 않는다 — 업로드는
	     성공하므로 릴리스 잡은 초록으로 끝나고, 빌드만 조용히 멈춘다.
	     DROP은 HTTPS(OS 제공 암호화)만 쓰므로 면제 대상이다.
	     apps/ios/Drop/Info.plist 가 같은 선언을 하고 있다. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>노트에 사진과 영상을 첨부하기 위해 사진 보관함에 접근합니다.</string>
	<key>NSCameraUsageDescription</key>
	<string>찍은 사진을 바로 노트로 만들기 위해 카메라를 사용합니다.</string>
	<!-- Flutter 기본 딥링킹을 끈다 (BRU-188).
	     켜져 있으면(3.27부터 기본값) 엔진이 `drop://note/1`을 **경로 문자열 그대로**
	     go_router에 넘겨 "no routes for location: drop://note/1"로 죽는다 — 위젯을
	     눌렀을 때 실제로 그 화면이 떴다. 이 앱의 딥링크 소비자는 app_links →
	     `DropLink.parse` → `DeepLinkRouter`(lib/app.dart)뿐이고, 그 경로는 이 키와
	     무관하게 그대로 동작한다. -->
	<key>FlutterDeepLinkingEnabled</key>
	<false/>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>com.intellieffect.drop.mobile.deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>drop</string>
			</array>
		</dict>
		<!-- Google 로그인 콜백 (BRU-186). GIDSignIn은 시작할 때 이 스킴이 번들에
		     등록돼 있는지 보고, 없으면 NSException으로 앱을 죽인다 — dart-define으로
		     넘기는 clientId만으로는 부족하다. 값은 make mobile-config가 만드는
		     ios/Flutter/GoogleSignIn.xcconfig에서 온다(커밋되지 않는 파일).
		     apps/ios/Drop/Info.plist가 같은 방식으로 같은 값을 넣는다. -->
		<dict>
			<key>CFBundleURLName</key>
			<string>com.intellieffect.drop.mobile.google</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>$(GOOGLE_IOS_CLIENT_ID_REVERSED)</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
